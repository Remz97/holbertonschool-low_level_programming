#include "main.h"
#include <stdio.h>
/**
 * add - Entry point
 * @a: int to be added to b
 * @b: int to be added to a
 * Return:0
 */
void print_to_98(int n)
{
	for (n = 0; n < 99; n++)
	{
		printf("%d, \n", n);
	}
}
